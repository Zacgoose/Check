# Content.js Blocking Flow - Visual Diagrams

## 1. High-Level Flow: From Page Load to Blocking

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PAGE LOAD                                    │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
                 ┌───────────────────────┐
                 │ initializeProtection() │
                 └───────────┬───────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │ runProtection()     │
                  │ (isRerun = false)   │
                  └──────────┬──────────┘
                             │
                 ┌───────────▼───────────┐
                 │   EARLY EXITS?        │
                 ├───────────────────────┤
                 │ • escalatedToBlock?   │
                 │ • User allowlist?     │
                 │ • Already running?    │
                 └───────┬───────────────┘
                         │ NO
                         ▼
            ┌────────────────────────────┐
            │   DOMAIN TRUST CHECKS      │
            ├────────────────────────────┤
            │ 1. Trusted login domain?   │─── YES ──> [Show Valid Badge] ──> EXIT
            │ 2. Microsoft domain?       │─── YES ──> [Log Event] ──────────> EXIT  
            │ 3. Exclusion system?       │─── YES ──> [Log Event] ──────────> EXIT
            └────────────┬───────────────┘
                         │ NO (Non-trusted)
                         ▼
            ┌────────────────────────────┐
            │  MICROSOFT LOGON DETECT    │
            │  isMicrosoftLogonPage()    │
            └────────┬───────────┬───────┘
                     │           │
                 NO  │           │  YES
                     │           │
      ┌──────────────▼──┐    ┌──▼──────────────────┐
      │ MS Elements?    │    │ Run Blocking Rules  │
      │ hasMicrosoftElements()  │  runBlockingRules()  │
      └──┬──────────┬───┘    └──┬──────────────────┘
         │          │           │
      NO │       YES│           ▼
         │          │      ┌─────────────────────┐
         │          │      │ Phishing Indicators │
         │          │      │ Detection Rules     │
         │          │      │ Combined Score      │
         │          │      └──────────┬──────────┘
         │          │                 │
         │          │      ┌──────────▼──────────┐
         │          │      │  Score < Threshold? │
         │          │      └──┬────────────────┬─┘
         │          │         │                │
         │          │      YES│             NO │
         │          │         │                │
         │          │    ┌────▼─────┐    ┌─────▼──────┐
         │          │    │  BLOCK   │    │   SAFE     │
         │          │    │  or      │    │  Monitor   │
         │          │    │  WARN    │    │            │
         │          │    └──────────┘    └────────────┘
         │          │
         │          └──────> [Check Phishing Indicators]
         │                           │
         │                    ┌──────▼─────────┐
         │                    │ Critical/High  │
         │                    │  Threats?      │
         │                    └──┬──────────┬──┘
         │                       │          │
         │                   YES │       NO │
         │                       │          │
         │                  ┌────▼──┐   ┌───▼────┐
         │                  │ BLOCK │   │ WARN   │
         │                  │  or   │   │   or   │
         │                  │ WARN  │   │ IGNORE │
         │                  └───────┘   └────────┘
         │
         └──────> [Setup Monitoring] ──> EXIT
```

---

## 2. Blocking Decision Flow

```
                ┌────────────────────────────────┐
                │   THREAT DETECTED              │
                │   (Low score or high threats)  │
                └──────────────┬─────────────────┘
                               │
                ┌──────────────▼──────────────┐
                │ Check Protection Enabled?   │
                │ (config.enablePageBlocking) │
                └────┬─────────────────┬───────┘
                     │                 │
                  YES│              NO │
                     │                 │
        ┌────────────▼──┐         ┌────▼──────────────┐
        │  Severity?    │         │  Show Warning     │
        └────┬──────┬───┘         │  Banner Only      │
             │      │              │  (No Blocking)    │
         HIGH│   MED│              └───────────────────┘
             │      │
    ┌────────▼──┐  └──────┐
    │  BLOCK    │         │
    │  PAGE     │    ┌────▼─────────┐
    └─────┬─────┘    │ WARN USER    │
          │          │ Show Banner  │
          │          └──────┬───────┘
          │                 │
          └─────────┬───────┘
                    │
        ┌───────────▼───────────┐
        │ showBlockingOverlay() │
        │        OR             │
        │  showWarningBanner()  │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────────┐
        │ Set escalatedToBlock=true │  (ONLY for blocking)
        └───────────┬───────────────┘
                    │
        ┌───────────▼───────────┐
        │  stopDOMMonitoring()  │  (ONLY for blocking)
        └───────────┬───────────┘
                    │
        ┌───────────▼──────────────────┐
        │  Redirect to blocked.html    │
        │  OR show overlay fallback    │
        └──────────────────────────────┘
```

---

## 3. DOM Monitoring Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    setupDOMMonitoring()                     │
└──────────────────────────┬──────────────────────────────────┘
                           │
                  ┌────────▼─────────┐
                  │ Already setup?   │
                  │ (domObserver?)   │
                  └──┬────────────┬──┘
                     │            │
                  YES│         NO │
                     │            │
              ┌──────▼──┐    ┌────▼──────────────────────┐
              │ RETURN  │    │ Create MutationObserver   │
              └─────────┘    │ observe documentElement   │
                             └────────┬──────────────────┘
                                      │
                             ┌────────▼────────────┐
                             │  DOM MUTATIONS      │
                             │  (Elements Added)   │
                             └────────┬────────────┘
                                      │
                          ┌───────────▼───────────┐
                          │ escalatedToBlock?     │
                          └──┬─────────────────┬──┘
                             │                 │
                          YES│              NO │
                             │                 │
                      ┌──────▼────┐      ┌─────▼──────────┐
                      │  IGNORE   │      │ Filter Added   │
                      │  (return) │      │ Nodes          │
                      └───────────┘      └──────┬─────────┘
                                                │
                                    ┌───────────▼────────────┐
                                    │ Skip injectedElements  │
                                    │ (Extension UI)         │
                                    └───────────┬────────────┘
                                                │
                                    ┌───────────▼─────────────┐
                                    │ Check if significant:   │
                                    │ • Form elements         │
                                    │ • Input fields          │
                                    │ • Scripts               │
                                    │ • MS-related content    │
                                    └───────────┬─────────────┘
                                                │
                                         ┌──────▼────────┐
                                         │ Significant?  │
                                         └──┬──────────┬─┘
                                            │          │
                                         NO │       YES│
                                            │          │
                                      ┌─────▼──┐  ┌────▼──────────────┐
                                      │ IGNORE │  │ Debounce (1000ms) │
                                      └────────┘  │ setTimeout(...)   │
                                                  └────┬──────────────┘
                                                       │
                                              ┌────────▼─────────────┐
                                              │ runProtection(true)  │
                                              │ (Re-scan page)       │
                                              └──────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  Automatic Timeouts                          │
├─────────────────────────────────────────────────────────────┤
│ • Fallback interval: 2000ms (checks for late-loading)       │
│ • Max monitoring time: 30 seconds (then stop)               │
│ • Debounce delay: 1000ms (consolidate rapid changes)        │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. Scan Rate Limiting Flow

```
                    ┌───────────────────────┐
                    │  runProtection(true)  │
                    │   (isRerun = true)    │
                    └──────────┬────────────┘
                               │
              ┌────────────────▼───────────────┐
              │   LAYER 1: Escalation Check   │
              │   if (escalatedToBlock)       │
              └────┬──────────────────┬────────┘
                   │                  │
                YES│               NO │
                   │                  │
            ┌──────▼─────┐   ┌────────▼──────────────┐
            │   RETURN   │   │ LAYER 2: Banner Check │
            │   (EXIT)   │   │ if (showingBanner)    │
            └────────────┘   └────┬──────────┬────────┘
                                  │          │
                               YES│       NO │
                                  │          │
                           ┌──────▼─┐  ┌─────▼──────────────────┐
                           │ RETURN │  │ LAYER 3: Scan Count    │
                           │ (EXIT) │  │ if (scanCount >= 5)    │
                           └────────┘  └────┬──────────┬─────────┘
                                            │          │
                                         YES│       NO │
                                            │          │
                                     ┌──────▼─┐  ┌─────▼───────────────┐
                                     │ RETURN │  │ LAYER 4: Cooldown   │
                                     │ (EXIT) │  │ if (now-lastScan <  │
                                     └────────┘  │     cooldown)       │
                                                 └────┬──────────┬─────┘
                                                      │          │
                                                   YES│       NO │
                                                      │          │
                                               ┌──────▼─┐  ┌─────▼────────────┐
                                               │ RETURN │  │ LAYER 5: Source  │
                                               │ (EXIT) │  │ if (!changed &&  │
                                               └────────┘  │ !threatRescan)   │
                                                           └────┬──────┬──────┘
                                                                │      │
                                                             YES│   NO │
                                                                │      │
                                                         ┌──────▼┐ ┌──▼──────┐
                                                         │RETURN│ │PROCEED  │
                                                         │(EXIT)│ │TO SCAN  │
                                                         └──────┘ └─────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    Cooldown Values                                   │
├─────────────────────────────────────────────────────────────────────┤
│ • Normal re-scan: 1200ms (SCAN_COOLDOWN)                            │
│ • Threat-triggered: 500ms (THREAT_TRIGGERED_COOLDOWN)               │
│ • Max threat re-scans: 2 (MAX_THREAT_TRIGGERED_RESCANS)             │
│ • Max total scans: 5 (MAX_SCANS)                                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 5. Exit Points Map

```
runProtection() Entry
│
├─ EXIT 1 (Line 3274): escalatedToBlock = true
│  └─> "Page already blocked - ignoring"
│
├─ EXIT 2 (Line 3282): isRerun && showingBanner
│  └─> "Banner already displayed - ignoring re-scan"
│
├─ EXIT 3 (Line 3339): User allowlist match
│  └─> "URL matches user allowlist"
│
├─ EXIT 4 (Line 3376): protectionActive && !isRerun
│  └─> "Protection already active"
│
├─ EXIT 5 (Line 3386): Rate limit exceeded
│  └─> "Scan count/cooldown exceeded"
│
├─ EXIT 6 (Line 3392): Page source unchanged
│  └─> "No DOM changes detected"
│
├─ EXIT 7 (Line 3467): Trusted login domain
│  └─> "Trusted Microsoft domain"
│      • Show valid badge
│      • Log legitimate access
│      • Setup minimal monitoring
│
├─ EXIT 8 (Line 3646): Microsoft domain (non-login)
│  └─> "Legitimate Microsoft domain (non-login page)"
│      • Log legitimate access
│      • No badge shown
│
├─ EXIT 9 (Line 3670): Domain exclusion
│  └─> "Domain in exclusion list"
│      • Log legitimate access
│
├─ EXIT 10 (Line 3735): No MS elements initially
│  └─> "Not Microsoft-related"
│      • Setup monitoring (for dynamic content)
│
└─ VARIOUS: After blocking/warning actions
   ├─ Line 3789: After critical threat block
   ├─ Line 3831: After critical phishing block
   ├─ Line 3893: After warning escalation block
   ├─ Line 4209: After blocking rule violation
   ├─ Line 4255: After client ID blocking
   ├─ Line 4331: After rogue app detection
   ├─ Line 4471: After low score block
   ├─ Line 4551: After very low score block
   └─ Line 4658: After high threat block (main path)
```

---

## 6. Threat-Triggered Re-scan Flow

```
┌─────────────────────────────────────────────────────────┐
│          Threats Detected During Scan                   │
│          (phishingResult.threats.length > 0)            │
└────────────────────────┬────────────────────────────────┘
                         │
              ┌──────────▼──────────┐
              │ !isRerun condition? │
              │ (Initial scan only) │
              └───┬──────────────┬──┘
                  │              │
               NO │           YES│
                  │              │
           ┌──────▼──┐   ┌───────▼──────────────────────┐
           │  SKIP   │   │ scheduleThreatTriggeredRescan │
           └─────────┘   │     (threatCount)             │
                         └───────┬──────────────────────┘
                                 │
                     ┌───────────▼────────────┐
                     │ Check re-scan count    │
                     │ (< MAX_THREAT = 2)     │
                     └───┬───────────────┬────┘
                         │               │
                      NO │            YES│
                         │               │
                  ┌──────▼───┐   ┌───────▼─────────────┐
                  │  SKIP    │   │ Check initial scan  │
                  │  (Limit) │   │ processing time     │
                  └──────────┘   └───┬────────────┬────┘
                                     │            │
                                  >5s│         <5s│
                                     │            │
                              ┌──────▼───┐ ┌──────▼────────────┐
                              │  SKIP    │ │ Schedule re-scan  │
                              │ (Slow=   │ │ • 800ms (1st)     │
                              │  legit)  │ │ • 2000ms (2nd)    │
                              └──────────┘ └──────┬────────────┘
                                                  │
                                         ┌────────▼─────────────┐
                                         │ setTimeout(...)      │
                                         │ runProtection(true)  │
                                         └──────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                 Why Threat-Triggered Re-scans?               │
├─────────────────────────────────────────────────────────────┤
│ Phishing pages often load malicious content gradually:      │
│ • Initial page may look clean                               │
│ • JavaScript injects credential forms later                 │
│ • External resources load asynchronously                    │
│                                                              │
│ Progressive delays give content time to load while          │
│ still detecting threats quickly.                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. Extension Element Tracking Flow

```
┌───────────────────────────────────────────────────┐
│         Extension Creates UI Element              │
│         (Banner, Overlay, Badge, etc.)            │
└───────────────────────┬───────────────────────────┘
                        │
            ┌───────────▼──────────────┐
            │ createElement()          │
            │ element.id = "..."       │
            │ element.innerHTML = ...  │
            └───────────┬──────────────┘
                        │
                        │ CRITICAL: Register BEFORE adding to DOM
                        │
            ┌───────────▼──────────────────┐
            │ registerInjectedElement(el)  │
            │ injectedElements.add(el)     │
            └───────────┬──────────────────┘
                        │
            ┌───────────▼───────────────┐
            │ document.body.appendChild │
            └───────────┬───────────────┘
                        │
            ┌───────────▼──────────────────┐
            │ Register all children too:   │
            │ el.querySelectorAll("*")     │
            │   .forEach(registerInjected) │
            └──────────────────────────────┘

┌───────────────────────────────────────────────────┐
│          Later: Scanning Page for Threats         │
└───────────────────────┬───────────────────────────┘
                        │
            ┌───────────▼──────────────┐
            │ getCleanPageSource()     │
            └───────────┬──────────────┘
                        │
            ┌───────────▼──────────────────┐
            │ Check injectedElements.size  │
            └───┬──────────────────┬───────┘
                │                  │
             = 0│               > 0│
                │                  │
         ┌──────▼────┐     ┌───────▼─────────────────┐
         │ Fast path │     │ Clone document          │
         │ Return    │     │ Build node map          │
         │ outerHTML │     │ Remove cloned versions  │
         └───────────┘     │ of injectedElements     │
                           └───────┬─────────────────┘
                                   │
                           ┌───────▼─────────────────┐
                           │ Return cleaned HTML     │
                           │ (No extension elements) │
                           └─────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  Why This Matters                            │
├─────────────────────────────────────────────────────────────┤
│ Without tracking, extension's own UI could trigger false    │
│ positives:                                                   │
│ • Warning banner might contain "Microsoft" text             │
│ • Overlay might have suspicious CSS patterns                │
│ • Badge could be detected as login element                  │
│                                                              │
│ Tracking ensures we only scan the actual page content.      │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. Complete State Machine

```
        ┌──────────────┐
        │   INITIAL    │
        │  (Page Load) │
        └──────┬───────┘
               │
               ▼
        ┌──────────────┐
        │   SCANNING   │◄──────────────┐
        │ (runProtection│               │
        │  in progress) │               │
        └───┬──────────┘                │
            │                           │
    ┌───────┴────────┐                  │
    │                │                  │
    ▼                ▼                  │
┌────────┐    ┌──────────────┐   ┌──────────┐
│TRUSTED │    │  ANALYZING   │   │DOM CHANGE│
│ DOMAIN │    │ (Threat      │   │DETECTED  │
│        │    │  detection)  │   │          │
└────────┘    └───┬──────────┘   └────┬─────┘
                  │                   │
         ┌────────┴─────────┐         │
         │                  │         │
         ▼                  ▼         │
    ┌─────────┐      ┌──────────┐    │
    │  SAFE   │      │ THREAT   │    │
    │         │      │ DETECTED │    │
    └────┬────┘      └────┬─────┘    │
         │                │           │
         ▼                ▼           │
    ┌─────────┐    ┌──────────┐      │
    │MONITOR  │    │DETERMINE │      │
    │  ONLY   │    │ SEVERITY │      │
    └────┬────┘    └────┬─────┘      │
         │              │             │
         │         ┌────┴────┐        │
         │         │         │        │
         │    ┌────▼──┐ ┌────▼───┐   │
         │    │WARNING│ │BLOCKING│   │
         │    │ BANNER│ │OVERLAY │   │
         │    └───┬───┘ └────┬───┘   │
         │        │          │        │
         │    ┌───▼─────┐    │        │
         │    │CONTINUE │    │        │
         │    │MONITOR  │    │        │
         │    └────┬────┘    │        │
         │         │         │        │
         └─────────┼─────────┘        │
                   │                  │
                   └──────────────────┘
                   (Re-scan on changes)
                            
                   ▼
            ┌──────────────┐
            │  ESCALATED   │
            │  TO BLOCK    │
            │ (Terminal)   │
            └──────────────┘
            No further scans

┌─────────────────────────────────────────────────────────────┐
│                  State Transitions                           │
├─────────────────────────────────────────────────────────────┤
│ SCANNING → TRUSTED        : isTrustedLoginDomain()          │
│ SCANNING → SAFE           : High legitimacy score           │
│ SCANNING → THREAT         : Low score or threats found      │
│ THREAT → WARNING          : Medium severity                 │
│ THREAT → BLOCKING         : High/critical severity          │
│ WARNING → MONITORING      : Setup DOM observer              │
│ MONITORING → SCANNING     : DOM changes detected            │
│ BLOCKING → ESCALATED      : escalatedToBlock = true         │
│ ESCALATED → (TERMINAL)    : No further state changes        │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. Performance Optimization Points

```
┌─────────────────────────────────────────────────────────────┐
│            Current Optimizations                             │
├─────────────────────────────────────────────────────────────┤
│ ✅ Regex caching (regexCache Map)                           │
│ ✅ Page source caching (1000ms TTL)                         │
│ ✅ DOM change debouncing (1000ms)                           │
│ ✅ Early exits (10+ exit points)                            │
│ ✅ Scan rate limiting (5 layers)                            │
│ ✅ Extension element filtering                              │
│ ✅ Progressive re-scan delays                               │
│ ✅ Slow page detection (skip rescans)                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│         Potential Optimization Opportunities                 │
├─────────────────────────────────────────────────────────────┤
│ 🟡 Consolidate domain trust checks (3 → 1 call)            │
│ 🟡 Cache element detection results                         │
│ 🟡 Unified page source caching strategy                    │
│ 🟡 Single setupDOMMonitoring() call point                  │
│ 🟡 Background Web Worker for heavy processing              │
└─────────────────────────────────────────────────────────────┘
```

---

## 10. Critical Path Timeline

```
Time 0ms: Page Load
    │
    ├─ Browser polyfill loads
    └─ content.js loads
          │
Time 100ms: initializeProtection()
    │      (small delay for DOM stability)
    │
    ├─> runProtection() starts
    │
Time 150ms: Load developer console setting
    │
    ├─> Load detection rules (from cache or fetch)
    │
Time 200ms: Domain trust checks
    │
    ├─ If trusted: EXIT (fast path ~200ms total)
    │
    └─ If suspicious: Continue to detection
             │
Time 300ms: isMicrosoftLogonPage() scan
    │
    ├─ Iterate through page elements
    ├─ Check regex patterns
    └─ Calculate weights
             │
Time 500ms: processPhishingIndicators()
    │
    ├─ Try background Web Worker
    │   └─ Fallback to main thread if fails
    │
    ├─ Scan page source for threats
    ├─ Calculate threat score
    └─ Return results
             │
Time 700ms: runDetectionRules()
    │
    ├─ Calculate legitimacy score
    └─ Combine with phishing score
             │
Time 800ms: Decision & Action
    │
    ├─ If blocking: showBlockingOverlay()
    │   ├─ Set escalatedToBlock = true
    │   ├─ stopDOMMonitoring()
    │   └─ Redirect to blocked.html
    │
    └─ If warning: showWarningBanner()
        ├─ Create banner element
        ├─ Register as injected
        └─ setupDOMMonitoring()

Time 1000ms+: DOM Monitoring Active
    │
    └─ Watch for dynamic content changes
       └─ Re-scan if significant changes detected

Time 30000ms: Monitoring timeout
    │
    └─ stopDOMMonitoring()
       └─ Clear intervals and observers

┌─────────────────────────────────────────────────────────────┐
│              Critical Path Notes                             │
├─────────────────────────────────────────────────────────────┤
│ • Fastest path (trusted): ~200ms                            │
│ • Typical path (safe): ~800ms                               │
│ • Slowest path (complex): ~2000ms                           │
│ • Blocking happens: ~1000ms                                 │
└─────────────────────────────────────────────────────────────┘
```

---

**Document Version:** 1.0  
**Last Updated:** 2025-12-05  
**Purpose:** Visual reference for content.js execution flow
